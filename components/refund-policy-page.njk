<section id="{{ componentId }}" class="component" data-component-id="{{ componentId }}">
  <div class="py-6 lg:py-8">
    {% if properties.title %}
      <h1 class="mb-4 text-center text-t-primary text-2xl font-bold pl-2">
        {{ properties.title | renderString }}
      </h1>
    {% endif %}
  
    <div class="max-w-3xl mx-auto">
      <div class="bg-card border border-white/5 rounded-lg text-t-primary/80 text-sm p-4 md:text-base md:p-6">
        <div class="editor">
          {{ refund_policy | safe }}
        </div>
      </div>
    </div>
  </div>
</section>